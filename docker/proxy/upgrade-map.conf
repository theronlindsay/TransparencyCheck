# http-level map used by the proxy_set_header Connection lines in the routing
# snippets: keeps WebSocket upgrades working without breaking keep-alive on
# ordinary requests.
map $http_upgrade $connection_upgrade {
	default upgrade;
	''      close;
}
