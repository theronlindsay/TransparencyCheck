name: Android Build & Release

permissions:
  contents: write

on:
  # Trigger manually
  workflow_dispatch:
  # Trigger when a tag starting with 'v' is pushed (e.g. v1.0.0)
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Dependencies
        run: npm ci
        working-directory: apps/client  

      - name: Generate Android Build Target
        run: npx cap add android
        working-directory: apps/client

      - name: Build Web Assets
        run: npm run build:client

      - name: Sync Capacitor
        run: npx cap sync android
        working-directory: apps/client

      - name: Create Icons
        run: npx @capacitor/assets generate
        working-directory: apps/client

      - name: Build Android APK (Debug)
        working-directory: apps/client/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: apps/client/android/app/build/outputs/apk/debug/app-debug.apk

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: apps/client/android/app/build/outputs/apk/debug/app-debug.apk
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
